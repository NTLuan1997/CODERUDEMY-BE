<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/static/css/users.css">
</head>

<body>
    <div class="user my-5">
        <div class="d-flex align-items-center justify-content-between">
            <h3 class="title-table">Thông tin người dùng</h3>
            <button class="btn btn-primary add-user" id="create-btn">
                <i class="fa fa-plus mr-2" aria-hidden="true"></i>
                <a href="/users/detail?type=create">Thêm mới</a>
            </button>
        </div>
        <table class="table mb-0" id="user-table">
            <thead class="thead-dark">
                <tr id="user-table-header"></tr>
            </thead>
            <tbody id="user-table-body">
                {{!-- {{#each users}}
                <tr>
                    <td>{{@index}}</td>
                    <td>{{this.user_name}}</td>
                    <td>{{this.email}}</td>
                    <td>{{this.password}}</td>
                    <td>{{this.status}}</td>
                    <td>{{this.age}}</td>
                    <td>
                        <button type="button" class="btn btn-primary">
                            <a href="/users/detail?type=update&id={{this._id}}">Sửa</a>
                        </button>
                        <button type="button" class="btn btn-danger btn-delete-user" data-toggle="modal"
                            data-whatever="{{this._id}}" data-id="{{this._id}}" data-target="#deleteUser">Xóa</button>
                    </td>
                </tr>
                {{/each}} --}}
            </tbody>
        </table>
    </div>

    <!-- Pagination -->
    <nav aria-label="Page navigation example">
        <ul class="pagination d-flex align-items-center justify-content-center">
            {{!-- <li class="page-item">
                <a class="page-link" href="#" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li> --}}
            <li class="page-items" data-id="1">1</li>
            <li class="page-items" data-id="2">2</li>
            <li class="page-items" data-id="3">3</li>
            {{!-- <li class="page-item">
                <a class="page-link" href="#" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li> --}}
        </ul>
    </nav>

    <script src="/static/js/user.js"></script>
</body>

</html>